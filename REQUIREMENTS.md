# 要件定義書

## 1. 目的

ブラウザ上で動作する「MMDビューワー兼、弾幕シューティング演出システム」を構築する。  
まずはフロントエンド中心で実装し、必要に応じてバックエンドを追加する。

## 2. システム概要

- フロントエンドは Next.js を使用する
- 3D描画は Three.js と MMDLoader を使用する
- MMDキャラとステージを表示する
- UI から演出を操作できるようにする
- 弾幕は 3D空間上で生成・移動・消滅できるようにする

## 3. 開発方針

- 初期段階ではバックエンドなしで進める
- 将来的に必要になった場合のみバックエンドを追加する
- バックエンド候補は Laravel または FastAPI とする
- Laravel は運用・管理向き、FastAPI は研究・実験向きとして使い分ける
- 言語やフレームワークは統一必須ではなく、責務分担を優先する

## 4. 対象範囲

- MMDモデルの表示
- ステージの表示
- モデル切り替え
- キャラやステージの追加・削除
- UI ボタンによる演出トリガー
- 弾幕の生成・更新・消滅
- エラー表示
- 状態表示

## 5. 機能要件

- [ ] MMDモデルを public 配下のURLから読み込めること
- [ ] モデルを実行時に切り替えられること
- [ ] キャラとステージをマウント・アンマウントできること
- [ ] UI 操作で弾幕演出を開始できること
- [ ] 弾幕は時間経過で動作すること
- [ ] 弾幕は複数パターンを持てること
- [ ] 読み込み失敗時にエラーを表示できること
- [ ] 現在の状態を画面上に表示できること
- [ ] 将来の拡張を見据えて処理を分離できること

## 6. 非機能要件

- ブラウザ上で単独動作できること
- 初期段階ではバックエンド依存にしないこと
- 描画処理は Three.js 側で保持すること
- React は UI と操作入力に専念すること
- 弾幕の大量生成でも破綻しにくいこと
- 将来の研究・運用拡張に耐えられること

## 7. 設計方針

- Next.js は画面と操作だけを担当する
- Three.js 側にシーン管理を持たせる
- MMDキャラ、ステージ、弾幕は独立した管理対象とする
- 弾幕は React state で大量管理しない
- 弾幕は描画ループ内で更新する
- 演出命令は UI からコマンドとして送る
- 弾幕の時間管理は経過時間ベースで行う

## 8. バックエンド方針

- 初期段階ではバックエンドを置かない
- 必要になったら Laravel または FastAPI を追加する
- Laravel はモデル管理、保存、認証、管理画面に向く
- FastAPI は弾幕生成、研究処理、数値計算、実験に向く
- どちらかに全面移行するのではなく、役割分担で共存してよい

## 9. データ管理方針

- モデルファイルは public 配下に置く
- 初期段階のモデル一覧はコードまたは静的JSONで管理する
- 永続保存が必要になった場合のみ DB を導入する
- 演出プリセットや設定保存が必要になった段階でサーバー側管理を検討する

## 10. 画面要件

- [ ] モデル選択UIを持つこと
- [ ] 再読み込みボタンを持つこと
- [ ] ステータス表示を持つこと
- [ ] エラー詳細を確認できること
- [ ] 将来的に演出開始、停止、切替の操作を追加できること

## 11. 弾幕要件

- [ ] 発射位置をキャラに紐づけられること
- [ ] 発射パターンを複数持てること
- [ ] 速度、角度、加速度、寿命を扱えること
- [ ] 時間経過で位置を更新できること
- [ ] 消滅条件を持てること
- [ ] パフォーマンスを意識して実装できること

## 12. MVP範囲

MVP（Minimum Viable Product）の最初の完成形として以下を目指す：

- 1体のMMDキャラ表示
- モデル切り替え
- 1種類以上の弾幕発射
- 弾幕の移動と消滅
- UI からの操作
- エラー表示
- バックエンドなしでの動作

## 13. 将来拡張

- モデル一覧の自動取得
- 演出プリセット管理
- リプレイ保存
- 複数キャラ表示
- 研究用の弾幕生成ロジック追加
- Laravel または FastAPI によるサーバー機能追加
- DB による設定保存
- ユーザー認証と管理

## 14. 成功条件

- [ ] ブラウザ上で MMD と弾幕演出が問題なく動く
- [ ] モデル切り替えができる
- [ ] 弾幕が UI 操作で発射できる
- [ ] バックエンドなしで最初の完成形が成立する
- [ ] 後から Laravel または FastAPI を足せる構造になっている

## 15. 技術スタック

| レイヤー | 技術 | 備考 |
|---------|------|------|
| フロントエンド | Next.js 16 + React 19 + TypeScript | ブラウザのみで動作 |
| 3D描画 | Three.js 0.152.2 | MMDLoader付属 |
| MMD対応 | MMDLoader + MMDAnimationHelper | three.jsのアドオン |
| バックエンド（将来） | Laravel または FastAPI | 現段階では不使用 |
| DB（将来） | 未定 | 必要になったら検討 |

## 16. プロジェクト構成

```
c:\Users\Sumi\Desktop\practice\three\MMD\
├── README.md
├── REQUIREMENTS.md (このファイル)
├── mmd/                    # MMDファイル配置
│   └── 紅魔館/
│       ├── 紅魔館.pmx
│       ├── 紅魔館.x
│       ├── かぼちゃ.x
│       ├── 外周の木.x
│       └── texture/
├── next-js/                # Next.jsプロジェクト
│   ├── app/
│   │   ├── page.tsx       # メインページ
│   │   └── layout.tsx
│   ├── component/
│   │   ├── MmdViewerCanvas.tsx      # Three.js描画
│   │   ├── MmdViewerPanel.tsx       # UI操作
│   │   ├── useMmdViewer.ts          # 描画ロジック
│   │   └── useMmdViewerStatus.tsx   # ステータス管理
│   ├── types/
│   │   └── three-mmd.d.ts
│   ├── public/
│   │   └── mmd/            # 公開用モデル置き場
│   └── package.json
├── src/                    # HTML版（参考用）
│   └── index.html
└── picture/                # 背景画像等
```

## 17. 実装進行計画

### フェーズ1: モデル選択機能（現在実装中）
- [x] 複数モデルの読み込み対応
- [x] モデル選択UI
- [ ] モデル切り替え

### フェーズ2: シーン管理基盤
- [ ] SceneController 実装
- [ ] ActorManager 実装
- [ ] オブジェクト登録・削除機能

### フェーズ3: 弾幕システム
- [ ] BulletSystem 実装
- [ ] パターン定義
- [ ] 時間ベースの制御

### フェーズ4: UI と連携
- [ ] 演出トリガーボタン
- [ ] パターン選択
- [ ] パラメータ制御

### フェーズ5: バックエンド検討
- [ ] 永続化が必要か判断
- [ ] Laravel or FastAPI 選定
- [ ] API 設計

---

**最終更新**: 2026年5月1日
